[package]
name = "arcrtc-implementation-real-device-tests"
version = "0.0.0"
edition = "2021"
rust-version = "1.96"
license = "Apache-2.0"
publish = false

[workspace]

[lints.rust]
unsafe_code = "forbid"
missing_docs = "warn"

[dependencies]
arcrtc-implementation-evidence = { path = "../../implementation-support/evidence" }
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
